<% content_for :title, "申込入力" %>

<section class="page-head">
  <div>
    <h2>申込入力</h2>
    <p class="section-lead">FAX 1枚分をそのまま入力します。伝道会コードは新コードで選択します。</p>
  </div>
</section>

<%= render "form", order: @order, submit_label: "申込を登録" %>
